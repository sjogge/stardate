{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build stardate-api",
            "type": "shell",
            "command": "cargo",
            "args": ["build", "--manifest-path", "${workspaceFolder}/src/stardate-api/Cargo.toml"],
            "problemMatcher": ["$rustc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
